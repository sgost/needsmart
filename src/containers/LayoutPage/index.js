import React from 'react';

import {
  Container,
} from './styles';

const LayoutPage = ({ history }) => {
  return (
    <Container>
      Home Page
    </Container>
  );
};

export default LayoutPage;
